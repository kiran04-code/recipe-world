<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Recipe Comments</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h2 class="mb-4 text-center">Comments on This Recipe</h2>
    <% if (coments && coments.length > 0) { %>
        <ul class="list-group">
          <% coments.forEach(comment => { %>
            <li class="list-group-item">
              <strong><%= comment.createBy?.name || comment.createBy?.email || 'Anonymous' %>:</strong>
              <span><%= comment.text || comment.coments %></span>
            </li>
          <% }) %>
        </ul>
      <% } else { %>
        <p class="text-center">No comments yet.</p>
      <% } %>
      
    <div class="text-center mt-4">
      <a href="/" class="btn btn-secondary">← Back to Recipes</a>
    </div>
  </div>
</body>
</html>
