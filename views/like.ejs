<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Recipe Likes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h2 class="mb-4 text-center">People Who Liked This Recipe</h2>

    <% if (likess.likedBy && likess.likedBy.length > 0) { %>
      <p class="text-center"><%= likess.likedBy.length %> like(s)</p>
      <ul class="list-group">
        <% likess.likedBy.forEach(user => { %>
          <li class="list-group-item">
            ❤️ <%= user.name || user.email || 'Unknown User' %>
          </li>
        <% }) %>
      </ul>
    <% } else { %>
      <p class="text-center">No likes yet.</p>
    <% } %>

    <div class="text-center mt-4">
      <a href="/allrecipes" class="btn btn-secondary">← Back to Recipes</a>
    </div>
  </div>
</body>
</html>
